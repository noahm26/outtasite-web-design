import { CoreAnimationDirective } from './core-animation.directive';

describe('CoreAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
