import { IfAnimatedDirective } from './if-animated.directive';

describe('IfAnimatedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAnimatedDirective();
    expect(directive).toBeTruthy();
  });
});
